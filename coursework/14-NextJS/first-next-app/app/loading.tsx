
// when there are async funtions to be run in the main component. then loading is rendered until it fetchs the component
export default function loading(){
    return <div>
        Loading../
    </div>
}